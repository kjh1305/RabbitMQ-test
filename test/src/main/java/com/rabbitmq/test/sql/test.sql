create table test.excel_file (
                                 id int auto_increment primary key not null,
                                 file_name varchar(256),
                                 file_type varchar(256)
);